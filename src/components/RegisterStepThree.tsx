import {IRegisterResponse} from "../interfaces/exportedInterfaces";

interface RegisterStepThreeProps {
    registerResponse: IRegisterResponse
}

export function RegisterStepThree({registerResponse}: RegisterStepThreeProps) {
    return (
        <h1>STEP THREE</h1>
    )
}